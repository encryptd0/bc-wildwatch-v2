# BC WildWatch Backend

This folder is reserved for a standalone backend service.

Suggested next steps:

1. Initialize a backend runtime (Node/Express, NestJS, Fastify, etc.).
2. Move API logic from `frontend/app/api` into this service when ready.
3. Add database integration and background notification processing.
