# BC WildWatch Frontend

This folder contains the Next.js frontend app for BC WildWatch.

## Recommended workflow

From the repository root:

```bash
npm install
npm run dev
```

## Frontend-only commands

If dependencies are already installed from root, you can still run app commands directly here:

```bash
cd frontend
npm run dev
npm run lint
```
